# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

- `E:\github code\vercel` 当前是空目录。不要假设这里已经存在 Next.js 脚手架、scripts、目录结构、测试配置或部署配置。
- 当前项目目标：使用 Next.js 编写一个展示二十四节气的页面，并最终部署到 Vercel。
- 在项目真正初始化之前，不要在计划或说明中写死 `dev/build/test/lint` 命令；先以仓库里实际生成的文件和 scripts 为准。